.*.swp
*~
/.project
/composer.lock
/vendor
/node_modules

# Build artifacts: univer.umd.js / univer.css are deliberately TRACKED in
# this repo so the extension can be dropped into a wiki without running
# `npm install` on the deploy host. Rebuild with `npm run build`.
/modules/univer/univer.umd.js.map
*.log
npm-debug.log*
